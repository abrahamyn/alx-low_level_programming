#include "main.h"

/**
 * _abs - check the code
 *
 * @int: integer parameter
 *
 * Return: Always 0.
 */
int _abs(int)
{
	int r;
	int n;

	n = r * -1;
	if (r < 0)
		_putchar(n);
	else
		_putchar(r);
}
