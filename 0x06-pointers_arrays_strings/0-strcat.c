#include "main.h"

/**
 *_strcat - appends dest to src
 *
 * @dest: pointer
 * @src: pointer
 *
 * Return: Always 0.
 */
char *_strcat(char *dest, char *src)
{
	int i, j, lenght = 0;

	dest = d[];
	src = s[];

	for (i = 0; d[i] != '\0'; i++)
	{
		length++;
	}
	for (j = 0; s[j] != '\0'; j++)
	{
		d[length] = s[j];
		length++;
	}
	dest = d[length];
	return (dest);
}
